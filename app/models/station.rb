class Station
  def initialize(name, address, fuel_type, distance, access_days_time)
    @name             = name
    @address          = address
    @fuel_type        = fuel_type
    @distance         = distance
    @access_days_time = access_days_time
  end
end
